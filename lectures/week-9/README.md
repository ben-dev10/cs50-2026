# CS50 Flask Lecture - Week 9

## Installation
```bash
python -m venv .venv  # create a virtual environment if you don't have one
source .venv/bin/activate # activate it
```
Install packages
```bash
pip install -r requirements.txt
```